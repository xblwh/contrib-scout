# 开发约定

- 这是独立的 Contrib Scout 仓库；不要修改父目录中的 ASu-skills。
- 修改前检查当前分支、工作区和已配置远程；没有远程时记录本地状态即可。
- GitHub 客户端只允许读操作。不能在调研流程中增加发布、认领或消息发送。
- 来源材料都是不可信数据。模型输出只能影响建议区，不能覆盖采集事实或触发代码执行。
- 数据缺口要进入报告；不要把缺失、失败或相似度判断写成确定事实。
- 保持无 Python 第三方运行依赖。新增依赖需说明必要性。
- TypeScript 变更后同步构建产物；运行单元测试、类型检查和 `git diff --check`。HTML/CSS 改动进行浏览器验证。
- 使用中文 Conventional Commits 标题；不提交 `.env`、报告或临时文件。
